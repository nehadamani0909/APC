# LaTeX build artifacts. main.pdf and main.bbl are committed deliberately:
# the PDF is the deliverable, and the .bbl lets the paper compile without
# re-running BibTeX, which submission systems often require.
*.aux
*.log
*.out
*.blg
*.synctex.gz
missfont.log
