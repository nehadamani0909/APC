# Handover: P9 → P10

## Completed

P9 is committed and pushed at `f330a10` (`Implement P9 evaluation and figures`).

Implemented in `frontier/eval/metrics.py`:

- CPGR, CPT, quality-vs-USD AUC, and regret helpers.
- Prompt-level paired BCa confidence intervals with 10,000 bootstrap resamples.
- Holm–Bonferroni step-down decisions.
- The APC-06 §14 minimum-detectable-effect calculation.

Implemented in `scripts/09_paper_figures.py`:

- One-command generation of P9 smoke tables and `reports/power.md`.
- Vector SVG outputs `reports/f1.svg` through `reports/f4.svg`.

## Verification

The repository currently passes:

- `pytest`: 35 passed.
- `ruff check .`: clean.
- `mypy frontier scripts tests`: clean.
- `python scripts/09_paper_figures.py`: succeeds and regenerates all P9 artifacts.

## Important scientific status

The P9 evaluator and figures are currently deterministic smoke artifacts using
synthetic values. They are plumbing and statistical-method tests, not paper
results. P10 must connect them to corpus-backed, held-out prompt-level rows and
real policy outputs before any result is reported as scientific evidence.

## P10 scope

Implement APC-06 experiments E4, E6, E7, E9, E10, and E11:

- E4 predictor and policy ablations.
- E6 cross-model/task/compressor transfer, including λ-only recalibration and
  risk violation under deliberate shift.
- E7 cross-compressor transfer.
- E9 cheapest-safe versus smallest-safe, including the `c_out/c_in` sweep.
- E10 net efficiency and the operating-window analysis.
- E11 failure taxonomy with counts and worked examples.

Required outputs are tables T4–T9 and `reports/failures.md` with at least
three worked examples per failure category. Negative results must be retained.

## Working-tree note

The pre-existing local corpus artifact `data/corpus/v1/corpus.parquet` and
`docs/corpus_datasheet.md` remain outside the P9 commit. Reports are ignored by
the repository policy and are regenerated by scripts.

Do not begin P10 until the user confirms this handover and authorizes the next
prompt.
